#include <iostream>
using namespace std;

int main() {
char a,z,A,Z,ch;
cout<<"enter the character "<<"\n";
cin>>ch;
if(ch>'a' && ch<'z' || ch>'A' && ch<'Z')
{ cout<<"it is an character "<<"\n";
}
else
{ cout<<"it is not an character "<<"\n";
}
return 0;
}
